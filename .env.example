# Discord Chess Bot - example environment file
# Copy to .env or export these variables in your environment

# Required
DISCORD_BOT_TOKEN=your-bot-token

# Optional
# Path to Stockfish binary; defaults to "stockfish" (in PATH)
STOCKFISH_PATH=stockfish

# SQLite database file path; defaults to "chessbot.db"
CHESSBOT_DB=chessbot.db
